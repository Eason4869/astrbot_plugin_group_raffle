# 更新日志 / CHANGELOG

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 规范。

## [0.4.0] - 2026-09-08

首个正式稳定版。整合 v0.3.1-beta ~ v0.3.9-beta 全部修复与新特性：

- **指令/指令组识别**：主命令「抽奖」（别名 raffle）下各子指令改为真实 handler 并统一分发，附未识别子命令兜底提示；行为管理默认权限与 README 对齐（开奖/模拟/模式/等次/定时/启用停用等默认仅管理员，状态/报名/取消报名/名单/定时预览/帮助默认所有人），AstrBot 管理员默认即插件管理员。
- **可视化管理面板（WebUI）**：修复「加载中」「Failed to fetch」「分页无响应」；独立打开自动携带仪表盘登录态直连；分群/全局保存、定时预览端到端可用；开关改打钩控件。
- **卡片渲染**：脱离 `astrbot_plugin_htmlrender`，改用 AstrBot 核心内置 `html_render`（`return_url=False` 落盘 PNG），浏览器不可用时自动降级 Pillow 简版 / 纯文本；卡片排版居中重做、去除显示为方框的 emoji、清晰度提升至 780px；帮助图标题同步去除 emoji。
- **真实 @ 中奖者**：通过平台实例类型名判断是否支持真实 @（修复适配器自定义 id 导致一直降级的问题）；开启卡片时 @ 作为独立消息先发送，未开卡片时置于文字版文首，不支持的平台自动降级文本。
- **开奖/模拟按配置等次**：数字参数不再覆盖已配置的中奖等次；模拟反馈更友好，结果与真实开奖一致（不写库、不清报名）。
- 跨群冷却开关、图片失败自动回退文本、唯一渲染文件；默认中奖模板表意更清晰；新增测试 `test_webapi.py` / `test_sim_card.py` / `test_status_card.py`。

## [0.3.9-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 修复
- **没有 @ 中奖者（真因）**：`unified_msg_origin` 首段是适配器【配置 id】（如用户自定义的
  “EasonBot”），并非类型名“aiocqhttp”，旧判断一直判为“不支持 @”。现改为通过 `context`
  找到该平台实例、读取其 `meta().name`（适配器类型名）判断是否支持真实 @，OneBot 系可正常 @。
- **卡片内 emoji 失效**：Pillow 使用的中文字体无彩色 emoji，会渲染成方框。卡片标题等图片内文字
  已改为纯文本（不再嵌入 🎊/🧪 等 emoji）。
- **卡片清晰度低**：Pillow 结果卡片分辨率由 520px 提升到 **780px**（字号同步放大），输出更清晰。

## [0.3.8-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 修复
- **中奖等次总显示“幸运奖”**：根因是 `抽奖 开奖 N`/`抽奖 模拟 N` 会用数字把已配置的等次
  覆盖成单个“幸运奖/模拟奖”。现开奖/模拟**始终按本群配置的等次开奖**（一等奖×1、二等奖×2 …），
  数字不再覆盖等次；帮助/README 同步去掉“开奖 [人数]”用法。已加回归断言。
- **没有 @ 中奖者**：`At(qq=...)` 改为优先使用整数 QQ 号；当开启卡片时，真实 @ 会作为
  **独立的一条消息**（文首）先发送（“恭喜中奖”），再单独发送卡片——@ 不再与图片挤在同一链里，
  确保能真正 @ 到人；未开启卡片时 @ 置于文字版文首。
- **中奖卡片内容挤在左上角**：结果卡片改为**优先用 Pillow 本地绘制**（520 宽居中横幅+分等次
  卡片块，布局完全可控）；HTML 模板（html_render 兜底路径）也改为内容自适应居中、不再固定
  520px 左对齐，避免在截图视口较宽时内容挤在左上角。

## [0.3.7-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 变更
- **移除 WebUI「分群配置-立即开奖」按钮**（在实际环境易因候选不足/图片 921 无效，改由群聊
  `抽奖 开奖` 触发）。
- `抽奖 状态`/`名单` 确定为**纯文本返回**（不再以卡片图片返回），同步修正 CHANGELOG/README
  中“以信息卡片返回”的相关表述。

### 修复/改进
- **真实 @**：重写发送逻辑——支持平台(OneBot 系)使用真实 `At(qq=...)` 组件并置于消息
  **文首**（参考 astrbot_plugin_bilibili：每个 At 后接空格）；文本占位符 `<winners>` 在有真实
  @ 时用纯昵称、不再写成 "@昵称"，消除“假性 @”。
- **卡片开启时不返回文字版**：开奖/模拟开启卡片时只发卡片图片；真实 @ 独立于卡片之外
  （置于图片前）。关闭卡片才返回文字版（文首真实@ + 文字）。
- **中奖卡片排版**：重做 HTML 卡片（顶部横幅 + 分等次卡片块 + 大号名字 + 备注区）与
  Pillow 兜底（同样的横幅/胶囊/居中布局），不再“文字挤在左上角”。

## [0.3.6-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 新增
- **跨群防连中冷却开关**（默认关闭）：开启后与所有群共享冷却，防止同一用户跨群连续中奖。
  可在管理面板「分群配置」勾选，或命令 `抽奖 冷却 跨群 开|关`。

### 修复
- **状态/名单等命令在图片发送失败时静默无返回**：信息卡片改为经 `context.send_message`
  直发图片；若渲染失败或图片上传失败（如 QQ highway 921），自动回退纯文本，保证命令一定
  有返回，不再出现“点了没反应/被 LLM 接管”。
- 开奖/模拟：整体(图片+文字)发送失败时先单独重发图片，仍失败再退纯文本，尽量展示卡片。
- 渲染输出改为每次唯一文件名，避免并发覆盖旧文件导致上传读到损坏文件。

## [0.3.5-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 修复
- **开奖/模拟发送失败 `'list' object has no attribute 'chain'`**（根治开奖/模拟
  “无返回”）：`context.send_message` 要求 `MessageChain`，此前传的是普通 list，
  AstrBot v4.28.0 会抛错，且错误兜底同样用 list 再次失败 → 群内无任何消息 →
  被 LLM 接管。已把所有直发改为 `MessageChain(chain=[...])`（notifier 开奖链
  与文本兜底、do_draw 定时失败与异常兜底）。
- `抽奖 状态`/`名单` 最终采用**纯文本返回**（QQ/NapCat 图片上传 921 不可靠，
  不再以图片卡片返回）；事件仍 stop_event 防止 LLM 接管。

## [0.3.4-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 修复
- **开关改为勾选（打钩）控件**：多次出现轨道式开关被主题/排版拉长变宽的问题，
  现改为紧凑的方框打钩 + 「已开启/已关闭」文字（原生 checkbox 状态，不可被拉长），
  点击即时切换文字；全局与分群配置所有开关统一生效。
- **`抽奖 状态`/`名单` 等命令未正常返回**：日志显示卡片已生成但 QQ/NapCat
  highway 上传报 `error_code=921`，图片发送失败导致命令“看起来没返回/被 LLM 接管”。
  现状态/名单/定时预览改为**纯文本可靠返回**（不再依赖图片上传）；事件仍 stop_event
  阻止 LLM 接管；开奖/模拟本就自带纯文本兜底，不受影响。

## [0.3.3-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 修复
- **命令被 LLM 接管（`抽奖 状态`/`模拟` 等未正常返回）**：AstrBot 中插件结果默认
  `result_type=CONTINUE`，只要事件未 `stop_event` 且未发生真实 send，进程会继续调用 LLM。
  现所有「抽奖」子命令在派发完成后统一调用 `event.stop_event()`，命令由插件完整接管，
  不再触发 LLM 闲聊；已加回归断言（状态/名单执行后必须 stop）。
- **WebUI 开关依然过长**：改用带前缀类名 `grs-sw`/`grs-track`/`grs-knob`（避免与页面/主题
  通用 `.switch`/`.slider` 样式冲突而把开关拉长），并缩小为 38×22 紧凑尺寸、轨道/圆钮高对比；
  容器 `grs-ctrl` 固定不拉伸。
- **WebUI 保存/立即开奖“无反应”**：为分群保存与立即开奖补 try/catch —— 请求失败(网络/桥接/500)
  不再静默，直接 toast 展示具体错误；开奖 0 成员群时有前置提示。

## [0.3.2-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 修复
- **管理面板开关显示**：开关底色/轨道过长是承载容器被 `flex:1` 拉伸所致；改为专用 `switch-ctrl` 容器（`flex:0 0 auto`），开关只占自身宽度、与标签垂直居中。
- **全局配置保存失效**：保存按钮把配置写进未定义变量 `st`（应为 `patch`），点击即抛错；已修正并补 try/catch，保存后清晰提示结果。
- **分群配置「立即开奖」& 状态/名单**：`抽奖 状态`、`抽奖 名单`、`定时预览` 为只读查询，现改为**群未启用也能查看**（不再被「未启用」拦截而看不到结果）；WebUI 分群保存为真实落库（经 SettingsStore 持久化并按需重建定时任务），立即开奖复用与群聊开奖同一 `do_draw` 路径，确保生效。

## [0.3.1-beta] - 2026-09-08

> 测试版：为便于与正式版区分，版本号带 `-beta` 后缀；验证稳定后再并入正式版。

### 新增
- **状态 / 名单信息整理**：`抽奖 状态`、`抽奖 名单`（报名名单 / 活跃榜 / 已观测成员）整理为清晰易读的文本返回（初期尝试与 `抽奖 帮助` 相同的信息卡片图片，后因 QQ/NapCat 图片上传 921 不可靠，最终确定为纯文本返回）。

### 修复
- **WebUI 开关显示**：全局配置「默认卡片」「默认@中奖者」及各群开关改为更清晰的绿色开关样式（圆圈加投影、底色对比更强），并修正与标签的垂直对齐（原来位置偏下）。
- **模拟开奖卡片**：确认并加固 `抽奖 模拟` 在卡片开启/关闭时都会发送卡片（模拟强制出图），核心 `html_render` 不可用时自动降级本地 Pillow 卡片，再降级纯文本。

### 变更
- `抽奖 卡片 开` 的反馈精简为「✅ 卡片渲染已开启。」，不再附带渲染实现说明。

## [Unreleased]

### 修复
- **指令与指令组无法识别**：AstrBot 实际只会按子指令精确匹配后直接调用对应子指令 handler，指令组父级方法不会被执行。已将原本仅作展示的空子指令改为真实 handler，统一委托到同一个内部分发逻辑（含未识别子命令兜底提示）。
- **CHANGELOG 版本日期有误**：0.1.0 / 0.2.0 / 0.3.0 实际均于 2026-09-08 发布，原日期已修正。
- **管理面板（WebUI）**：修复在 AstrBot 嵌入页 / 独立打开时一直「加载中」且点击分页无响应的问题；独立打开时改为自动携带仪表盘登录态（localStorage token / cookie）直连 `/api/plug/...`，彻底消除 `Failed to fetch`。
- 默认中奖消息模板表意更清晰，`<contact>` 拼接不再产生歧义。
- **帮助图标题**：移除标题中的 emoji（部分渲染环境下会显示为“方框”），HTML 模板与 Pillow 兜底同步清理。
- **卡片渲染说明**：`抽奖 卡片 开` 的提示语更新——现已脱离 `astrbot_plugin_htmlrender`，改用 AstrBot 核心内置 `html_render`（浏览器不可用时自动降级为本地 Pillow 简版卡片 / 纯文本）。
- **模拟开奖反馈更友好**：本群还没有成员数据时，`抽奖 模拟` 不再返回冷冰冰的开奖失败，而是给出清晰的模拟提示。

### 变更
- 行为管理中的默认权限与 README 对齐：开奖/模拟/模式/等次/冷却/排除管理员/艾特/卡片/模板/窗口/定时/启用/停用默认**仅管理员**，状态/报名/取消报名/名单/定时预览/帮助默认**所有人**。这些默认权限同样以 AstrBot 原生 `PermissionTypeFilter` 注册，仪表盘「管理行为」页面会正确展示。
- AstrBot 管理员（宿主/配置的管理员）默认即视为本插件管理员；同时兼容按消息角色识别群主/群管。
- `抽奖 模拟` 现在与真实开奖完全一致地发送开奖消息：渲染卡片（模拟时强制出图）、@ 中奖者、套用中奖模板与等次信息，标题标注「模拟开奖（不记录）」；仍然不写中奖记录、不写开奖流水、不清空报名，可放心反复测试。

## [0.3.0] - 2026-09-08

### 新增
- 🎛️ **可视化管理面板（WebUI 插件页）**：在 AstrBot 面板中打开「群抽奖助手」页面，包含四个标签：
  - **总览**：启用群 / 定时群 / 观测成员 / 开奖次数 / 中奖人次等统计卡片 + 最近中奖列表
  - **中奖记录**：全部群 / 按群号筛选的中奖历史（时间、群、中奖者、等次）
  - **全局配置**：白黑名单、管理员、默认模板、联系方式、默认卡片/@、防刷参数等在线修改
  - **分群配置**：群列表 + 单群完整配置（模式、等次、冷却、活跃窗口、模板、卡片/@、定时），支持在线保存与「立即开奖」
- 🖼️ **帮助命令图片化**：`抽奖 帮助` 现在把命令表渲染成图片发送（核心内置 html_render，失败自动降级文本），命令表内容与 README 单一数据源同步
- 🧩 卡片与帮助图改为使用 **AstrBot 核心内置 `html_render`**（Jinja2 模板 `templates/*.html`），**不再依赖 astrbot_plugin_htmlrender 插件**；核心不可用时降级 Pillow，再降级纯文本

### 变更
- 🏗️ 命令结构改为**指令组 + 子指令**：主命令组「抽奖」，`raffle` 为别名；行为管理中各子指令（开奖、定时、模式…）以「抽奖 xxx」中文独立列出，便于单独开关
- 指令组裸命令（仅发送「抽奖」）与各子命令统一由父组分发，帮助/配置逻辑不变

### 修复
- 配置界面长说明显示不全（长说明移至 `hint`，字段标签精简；`list` 类型去除非法 `items`；长文本改用 `text`）

## [0.2.0] - 2026-09-08

### 新增
- 插件图标 `logo.png`，插件市场 / 面板显示中文展示名「群抽奖助手」（`display_name`）
- 中文主命令「抽奖」（`raffle` 为别名），修复此前仅 `/raffle` 生效的问题
- **模拟开奖**：`抽奖 模拟 [人数] [轮数]`，不写库、不 @、不发卡片、不清报名，可反复测试

### 变更
- README 头部增加 logo 与浏览量徽章

## [0.1.0] - 2026-09-08

### 新增
- 🎰 首个版本：
  - **分群独立配置**：定时、等次名额、参与模式、模板、卡片开关等按群持久化，重启自动恢复
  - 三种参与模式：全员等权 / 活跃度加权（√压缩 + 防刷）/ 报名参与
  - 多等次、多名额；同一人不重复中奖；防连中冷却；可选排除管理员
  - 定时开奖：每日 / 每周 / 每两周 / 每月 / 自定义 cron，支持下次触发预览；停机不补发
  - @ 中奖者（不支持的平台自动降级文本昵称）
  - 自定义中奖模板占位符：`<winners> <count> <group> <time> <contact>`
  - 开奖卡片渲染（@ 不进卡片）
  - 群白名单 / 黑名单 + 群级启用/停用
  - 活跃度仅统计条数、不保存消息正文，支持自动清理
